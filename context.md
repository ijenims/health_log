# Health Log セッション引継ぎメモ

最終更新：2026-08-03

## 1. プロジェクト概要

過去の健康診断結果を年月単位で時系列表示する個人用Webアプリ。

- アプリ名：Health Log
- 対象ユーザー：本人1人
- 想定端末：iPhone SE2優先、PC対応
- 実データ：約1989年〜2026年、59回分
- UI：日本語、外部UIフレームワークなし

## 2. 技術構成

- React
- Vite
- JavaScript
- Recharts
- CSS
- IndexedDB（ブラウザ内の実データ保存）
- 現在はバックエンドなし

```bash
npm install
npm run dev
npm run build
```

## 3. 主なファイル

```text
src/
├─ App.jsx                 # 画面、グラフ、データ読込み
├─ dataStore.js            # IndexedDB操作
├─ styles.css
└─ data/
   ├─ item-master.json     # 分類、項目、単位、別名、基準値
   └─ measurements.json    # 公開可能なサンプルデータ

scripts/
└─ prepare-health-data.mjs # 原本CSVをアプリ用JSONへ変換

private-data/              # 実データ。Git管理対象外
├─ originals/
│  └─ my_data_sheet.csv
├─ imports/
│  └─ measurements.json
├─ exports/
└─ backups/
```

旧`src/data/health-checks.json`は削除済み。IDEのタブに残っていても現行ファイルではない。

## 4. 実データの取扱い

- 原本CSVは`private-data/originals/my_data_sheet.csv`。
- 原本は変更・削除しない。
- `.gitignore`で`private-data`内の実データを除外済み。
- 原本の2行目`limit_upper`を最新上限値、3行目`limit_lower`を最新下限値として使用する。
- 過去時点の基準値は使わず、最新基準値を全期間へ適用する方針。
- ピロリ菌データは取込み対象外。
- 実データ変換：

```bash
npm run prepare:data
```

- 生成先：`private-data/imports/measurements.json`
- ブラウザの「データを読み込む」から生成JSONを選択する。
- 読込み後はIndexedDBへ保存され、次回起動時に自動復元される。
- 「サンプルに戻す」でIndexedDBの実データを削除できる。
- 変換JSONを更新した場合、ブラウザで再読込みが必要。

## 5. 派生項目

- BMI：`体重kg ÷ 身長m²`、小数1桁。同じ健診年月に身長と体重がある場合のみ算出。
- ペプシノゲン比：`ペプシノゲンⅠ ÷ ペプシノゲンⅡ`、小数2桁。両値がある場合のみ算出。
- BMI：29回分。
- ペプシノゲンⅠ・Ⅱ・比：各17回分。

## 6. 現在の画面構成

画面部分は一旦完成扱い。

1. 分類を選ぶ。
2. 分類内の検査項目ごとにグラフカードが縦に並ぶ。
3. 画面を縦スクロールして各グラフを確認する。

廃止済み：

- 「検査項目を選ぶ」操作
- 「選択中の項目」表示
- 独立したグラフタイトル

各グラフカード上部のサマリーカードがタイトルを兼ねる。表示内容は次だけ。

- 検査項目名
- 最新の有効測定値
- 単位

表示しない情報：

- 年月
- 前回比
- 基準内・高値・低値ステータス
- 最小値・最大値

検査項目名・最新値・単位は横1行で表示する。

## 7. グラフのグループ

- 原則：1検査項目＝1グラフ。
- 血圧のみ、収縮期血圧と拡張期血圧を同じグラフに2系列表示。
- 胃部検査：
  - ペプシノゲンⅠ・Ⅱを同じグラフに2系列表示。
  - ペプシノゲン比は別の単独グラフ。

項目色は選択順ではなく`itemCode`に固定。

```text
エメラルド   #08a878
オレンジ     #f07945
ブルー       #3688d8
バイオレット #8a6edb
ピンク       #e55f91
```

## 8. 長期グラフ仕様

- 表示期間は画面内の全グラフ共通。
- 選択肢：5年、10年、全期間。
- 初期表示：10年。
- 「表示期間」と期間ボタンの文字サイズ：16px。
- 長期間はグラフごとに横スクロール。
- 期間変更時、各グラフを最新側へ自動スクロール。
- 横軸はUTCタイムスタンプによる実年月スケール。
- 年月表示形式：`YYYY/MM`（例：`2026/07`）。
- 半年間隔は1年間隔のおよそ半分の距離になる。
- ツールチップ内の年月は日本語表記。
- 欠測時は点を描画しない。
- 欠測区間は無視し、前後の実測値を直線で接続する（`connectNulls`）。
- 12点以下の場合だけ測定値ラベルを常時表示する。

## 9. 縦軸仕様

- 縦軸はグラフ本体と分離し、左端に固定。
- 横スクロールしても数値を残す。
- 固定軸は白背景。フェードは線を隠すため使用しない。
- グラフ本体は左58pxの余白を確保。
- 通常の目盛りは`1・2・5 × 10の累乗`から読みやすい間隔を自動選択。
- 固定縦軸と水平グリッドは同じ目盛値を使用。
- 血圧：グラフ高さを通常の2倍、目盛間隔20mmHg固定。
  - PC：680px
  - iPhone SE2：520px
- 身長：縦軸範囲170〜180cm、2cm間隔固定。

## 10. 基準範囲仕様

- 最新の基準値を全期間へ適用する。
- 基準範囲内ではなく、範囲外の背景だけを薄く着色する。
- 基準内の点：系列色の輪郭＋白抜き。
- 基準値外の点：赤い輪郭＋赤い塗りつぶし。
- サマリーカードには判定ステータスを表示しない。
- 基準値がない項目は通常表示する。

## 11. 分類・実データ項目

分類：

- 身体
- 血圧
- 脂質
- 肝機能
- 腎機能
- 糖代謝
- 尿酸
- 血球
- 尿検査
- 前立腺
- 胃部検査

実データに合わせて追加した主な項目：

- ヘマトクリット
- ChE
- ALP（IFCC）
- ALP（旧測定法）
- 高感度PSA
- ペプシノゲンⅠ
- ペプシノゲンⅡ
- ペプシノゲン比

新旧ALPは測定法と基準値が異なるため別項目。HbA1cは変換時にNGSPを優先し、なければ旧HbA1c列を使用する。

## 12. データ構造と将来方針

内部では共通`itemCode`を使用する。

```text
AST / GOT → ast
ALT / GPT → alt
γ-GTP / γ-GT / GGT → gamma_gtp
```

現在の正規化データ：

```json
{
  "referenceRanges": {
    "systolic": { "lower": null, "upper": 129 }
  },
  "measurements": [
    {
      "examinationDate": "2026-07",
      "values": { "systolic": 120, "diastolic": 74 }
    }
  ]
}
```

将来のAWS構成案：

```text
React / Vite
  → Amplify Hosting
  → Cognito
  → API Gateway
  → Lambda
  → DynamoDB
  → S3 CSVバックアップ
```

- Cognitoは管理者作成の1ユーザー、自己登録禁止。
- 本番公開時は認証なしAPIを公開しない。
- DynamoDBでは1検査項目・1測定値を1レコードとする構想。

## 13. データ管理機能

実装済み：

- ヘッダーの「データ管理」ボタン。
- 現在の全データをJSONでバックアップ書出し。
- 現在の全データを縦長CSVでバックアップ書出し。
- 健診年月と検査値の新規登録。
- 未測定項目は空欄で登録可能。
- 同一年月の重複新規登録を防止。
- 身長・体重入力時にBMIを自動計算。
- ペプシノゲンⅠ・Ⅱ入力時に比を自動計算。
- 「新規追加／既存データ編集」の切替。
- 既存年月を選択して測定値を修正。
- 確認ダイアログ付きの年月単位削除。
- 追加・修正・削除後にIndexedDBとグラフへ即時反映。
- iPhone SE2ではデータ管理画面を全画面表示。

JSON取込みプレビューも実装済み：

- JSON選択時に即時保存しない。
- ファイル名、取込み件数、期間、現在と同じ年月の件数を表示。
- 現在のデータを全件置換することを明記。
- キャンセル可能。
- 確認後にIndexedDBへ保存。

### 次回最初に行う受入確認

取込みプレビューはコード実装とビルド確認まで完了しているが、ブラウザ実操作テストは未完了。次回はAWSへ進む前に以下を確認する。

1. `npm run dev`で起動し、`http://127.0.0.1:5173/`を開く。
2. 「データ管理」→「JSON書出し」で現在データをバックアップ。
3. 「データを読み込む」から`private-data/imports/measurements.json`を選択。
4. プレビューに59回、1989-04〜2026-07、重複件数が表示されることを確認。
5. 一度「キャンセル」し、表示データが変化しないことを確認。
6. 再度選択して「全データを置換」。
7. 「59回分を読み込みました」と各グラフの表示を確認。
8. ブラウザ再読み込み後も「実データ」とグラフが維持されることを確認。

## 14. 次の開発候補

画面とローカルデータ管理は一旦完成。取込みプレビューの受入確認後、AWS側へ進む。

候補：

1. 取込みプレビューのブラウザ受入確認
2. AWS SAM用`backend/`フォルダ作成
3. DynamoDB、Lambda、API GatewayのCRUD API
4. Cognito認証
5. フロントのIndexedDB/API切替
6. 実データ移行
7. Amplify Hosting公開

## 15. GitHub・検証状態

- リポジトリ：`https://github.com/ijenims/health_log.git`
- ブランチ：`main`
- 直近のプッシュ済みコミット：`85cc8ff Improve long-term health chart visualization`
- コミット`5c3914e`までGitHubへプッシュ済み。
- それ以降のデータ管理、編集・削除、取込みプレビュー、今回の`context.md`更新は未プッシュ。
- 各主要変更後に`npm run build`を実行済み。
- 直近の既存データ編集・削除、取込みプレビュー実装後もビルド成功。
- Viteのバンドルサイズ警告はあるが、ビルドエラーではない。

## 16. AWSバックエンド実装・ローカル検証（2026-08-05）

- `backend/template.yaml`にAWS SAM構成を追加済み。
- Cognito User Poolは自己登録禁止の管理者作成方式。
- API Gateway HTTP APIはCognito JWT認証必須。
- Lambda（Node.js 22 / arm64）に健診測定値の取得・登録・更新・削除を実装。
- DynamoDBはオンデマンド、暗号化、ポイントインタイムリカバリ、削除時保持を設定。
- Lambda依存パッケージをインストール済み。
- Lambda単体テスト2件、構文確認、フロントの`npm run build`に成功。
- AWS SAM CLI 1.165.0を導入済み。
- `sam validate --lint`と`sam build`に成功。
- AWSへのデプロイと実データ送信は未実施。

### ローカル開発方針

- この規模ではDocker Desktopを導入しない。
- フロントは`npm run dev`、Lambdaは単体テスト、インフラとLambda成果物はSAM検証・ビルドで確認する。
- APIとの結合確認は、小規模なAWS開発環境へデプロイして行う。
- AWS CLI 2.36.16を導入済み。AWS認証設定とデプロイは未実施。

### 次の作業

1. AWSアカウント、利用リージョン、予算アラート方針を確認。
2. AWS CLIで認証設定。
3. CognitoはMFAなし・自己登録禁止の1ユーザー構成とする。
4. SAMを開発環境へ初回デプロイ（実データはまだ移行しない）。
5. テストユーザーでCRUD APIを確認。
6. フロントにCognitoログインとIndexedDB/API切替を追加。

## 17. セッション終了時点（2026-08-05）

- 取込みプレビューはブラウザで受入確認済み。59回分の同一データ置換と、重複年月4件のプレビュー表示を確認した。
- AWS SAM CLI 1.165.0はWindowsへ導入済み。現在のシェルでPATHが未反映の場合は`C:\Program Files\Amazon\AWSSAMCLI\bin\sam.cmd`を使用する。
- SAMテンプレートのlint検証とLambda成果物のビルドは成功済み。生成先`.aws-sam/`はGit管理対象外。
- AWS CLI 2.36.16は`C:\Program Files\Amazon\AWSCLIV2\aws.exe`へ導入済み。
- Docker Desktopはこのアプリでは導入しない方針。`sam local`は使わず、Lambdaの単体テストとAWS開発環境で検証する。
- AWS認証情報は未設定。AWSリソースの作成、課金開始、実データのアップロードは一切行っていない。
- バックエンド、データ管理画面、取込みプレビュー、関連ドキュメントの変更はまだコミット・プッシュしていない。
- 次回は、AWSリージョン・認証方法・予算アラート・Cognito MFA方針を決めてから初回デプロイへ進む。

### Health Log認証方針

- 現段階は本人だけが使う個人用サービスのため、Health Log側のMFAは使用しない。
- Cognitoの自己登録は禁止し、管理者が作成した1ユーザーだけを利用する。
- パスワード管理機能で生成・保存する、20文字以上かつ他サービスで未使用の固有パスワードを使う。
- パスワードを保管するGoogleまたはAppleアカウント側はMFAで保護する。
- AWSアカウントのルートユーザーにはMFAを設定する。Health LogのCognito認証情報とAWS管理者認証情報は分離する。
- 将来、他人の健診データを預かるサービスへ変更する場合は、公開前にMFA、監査ログ、権限管理、運用・復旧手順を再設計する。

## 18. AWSルートユーザーMFA設定（2026-08-06）

- AWSルートユーザーへパスキーMFAを登録済み。
- Windows PCとiPhoneを使ったクロスデバイス認証で登録した。
- PCの通常ブラウザで「別のデバイス／スマートフォン」を選び、表示された連携用QRコードをiPhoneで読み取る方式。
- パスキーはiPhone側のChrome／Googleパスワードマネージャー経由で処理されたと考えられる。
- 登録後、いったんログアウトし、同じクロスデバイス方式でルートユーザーへ再ログインできることを確認済み。
- QRコード読取り後はiPhone側で追加操作を求められず、そのまま認証が完了した。

### MFA設定時に判明した注意点

- AWS IAMはWindows Helloへのローカル・パスキー登録をサポートしていないため、この方法は使わない。
- Bitwardenブラウザ拡張がWebAuthn処理へ割り込む場合がある。クロスデバイス登録時は必要に応じて一時無効化する。
- InPrivate／シークレットモードは使わず、通常のChromeまたはEdgeを使用する。
- 認証アプリ用QRコードと、パスキーのクロスデバイス連携用QRコードは別物。今回利用したのは後者。
- Googleパスワードマネージャー内のAWS関連項目は、動作確認済みのため無理に整理・削除しない。
- AWSルートユーザーはアカウント管理専用とし、Health Log開発の日常作業には使用しない。

### 次の開始地点

1. ルートユーザーで開発用の非ルート認証環境を準備する。
2. 予算アラートと東京リージョン`ap-northeast-1`を確認する。
3. AWS CLIを一時認証で接続する。
4. 偽データ用のAWS開発環境をSAMで初回デプロイする。

## 19. AWS開発環境・Cognitoログイン（2026-08-06）

### AWS CLI・費用管理

- IAMユーザーを使い、`aws login --profile health-log-dev`によるブラウザベースの一時認証を設定済み。
- 長期Access Key / Secret Access Keyは作成・保存していない。
- CLI既定リージョンは東京`ap-northeast-1`。
- IAMユーザーには現状`AdministratorAccess`が付与されている。初期構築完了後にHealth Log用の最小権限へ絞る候補。
- AWS Budget `HealthLog-Monthly`を月額5 USDで作成済み。
- 簡易テンプレートにより、実績85%・100%および予測100%のメール通知を設定済み。
- CLI調査時に一時セッショントークンがデバッグ出力へ表示されたため、直ちに`aws logout`を実行して旧セッションを無効化し、再認証した。
- 旧アクセストークンは最大15分で失効。長期キーの漏えいはなく、以後AWS CLIで`--debug`を使用しない。

### SAM初回デプロイ

- スタック名：`health-log-dev`。
- `backend/samconfig.toml`を生成済み。リージョン、スタック名、`CAPABILITY_IAM`などの再デプロイ設定を保持する。
- CloudFormationスタックは`CREATE_COMPLETE`。
- 作成済みリソース：Cognito User Pool / User Pool Client、API Gateway HTTP API、Lambda、Lambda実行ロール、DynamoDB。
- EC2、RDS、NAT Gatewayなどの常時稼働型リソースは使用していない。
- 未認証で`GET /measurements`を呼ぶとHTTP 401になることを確認済み。
- AWSへの健診実データ送信はまだ行っていない。DynamoDBは空のまま。

### フロントのCognito認証

- `aws-amplify`を追加し、既存Cognito User Poolへ直接接続。
- 実AWSの公開設定値はGit対象外の`.env.local`へ保存。
- Git管理用に`.env.example`を追加。
- 未ログイン時はHealth Log本体を表示せず、日本語の最小ログイン画面だけを表示する。
- 自己登録UIは設けない。Cognito管理者作成ユーザーだけがログイン可能。
- 初回ログイン時の`CONFIRM_SIGN_IN_WITH_NEW_PASSWORD_REQUIRED`に対応。
- ブラウザのパスワード自動生成・自動入力でReact stateが更新されない場合に備え、送信時は`FormData`から実際の入力値を読む。
- ログイン後はヘッダーに`AWS接続`表示と`ログアウト`ボタンを表示。
- Cognitoから届いた仮パスワードでログインし、恒久パスワードへ変更後、既存アプリが正常表示されることをブラウザで確認済み。
- 変更後の`npm run build`は成功。Amplify追加によりバンドルサイズ警告は増えたが、ビルドエラーではない。

### 現在のデータ状態

- ログイン後に表示している健診データは、引き続きブラウザ内IndexedDBのデータ。
- Cognito認証とAWS APIは用意できたが、フロントのAPI読込み・書込み切替は未実装。
- 実データのAWS移行は未実施。

### 次の開始地点

1. 認証済みJWTを付けるAPIクライアントを実装。
2. DynamoDBが空であることを画面上でも判別できるようにする。
3. IndexedDB実データからAWSへの移行プレビューを作る。
4. 件数・期間・上書き内容を確認後に実データを移行する。
5. API版で取得・追加・修正・削除を受入確認する。
6. 問題がなければIndexedDBをローカルバックアップ／復旧用途へ整理する。
