# Health Log セッション引継ぎメモ

最終更新：2026-08-03

## 1. プロジェクト概要

過去の健康診断結果を年月単位で時系列表示する個人用Webアプリ。

- アプリ名：Health Log
- 対象ユーザー：本人1人
- 想定端末：iPhone SE2優先、PC対応
- 実データ：約1989年〜2026年、59回分
- UI：日本語、外部UIフレームワークなし

## 2. 技術構成

- React
- Vite
- JavaScript
- Recharts
- CSS
- IndexedDB（ブラウザ内の実データ保存）
- 現在はバックエンドなし

```bash
npm install
npm run dev
npm run build
```

## 3. 主なファイル

```text
src/
├─ App.jsx                 # 画面、グラフ、データ読込み
├─ dataStore.js            # IndexedDB操作
├─ styles.css
└─ data/
   ├─ item-master.json     # 分類、項目、単位、別名、基準値
   └─ measurements.json    # 公開可能なサンプルデータ

scripts/
└─ prepare-health-data.mjs # 原本CSVをアプリ用JSONへ変換

private-data/              # 実データ。Git管理対象外
├─ originals/
│  └─ my_data_sheet.csv
├─ imports/
│  └─ measurements.json
├─ exports/
└─ backups/
```

旧`src/data/health-checks.json`は削除済み。IDEのタブに残っていても現行ファイルではない。

## 4. 実データの取扱い

- 原本CSVは`private-data/originals/my_data_sheet.csv`。
- 原本は変更・削除しない。
- `.gitignore`で`private-data`内の実データを除外済み。
- 原本の2行目`limit_upper`を最新上限値、3行目`limit_lower`を最新下限値として使用する。
- 過去時点の基準値は使わず、最新基準値を全期間へ適用する方針。
- ピロリ菌データは取込み対象外。
- 実データ変換：

```bash
npm run prepare:data
```

- 生成先：`private-data/imports/measurements.json`
- ブラウザの「データを読み込む」から生成JSONを選択する。
- 読込み後はIndexedDBへ保存され、次回起動時に自動復元される。
- 「サンプルに戻す」でIndexedDBの実データを削除できる。
- 変換JSONを更新した場合、ブラウザで再読込みが必要。

## 5. 派生項目

- BMI：`体重kg ÷ 身長m²`、小数1桁。同じ健診年月に身長と体重がある場合のみ算出。
- ペプシノゲン比：`ペプシノゲンⅠ ÷ ペプシノゲンⅡ`、小数2桁。両値がある場合のみ算出。
- BMI：29回分。
- ペプシノゲンⅠ・Ⅱ・比：各17回分。

## 6. 現在の画面構成

画面部分は一旦完成扱い。

1. 分類を選ぶ。
2. 分類内の検査項目ごとにグラフカードが縦に並ぶ。
3. 画面を縦スクロールして各グラフを確認する。

廃止済み：

- 「検査項目を選ぶ」操作
- 「選択中の項目」表示
- 独立したグラフタイトル

各グラフカード上部のサマリーカードがタイトルを兼ねる。表示内容は次だけ。

- 検査項目名
- 最新の有効測定値
- 単位

表示しない情報：

- 年月
- 前回比
- 基準内・高値・低値ステータス
- 最小値・最大値

検査項目名・最新値・単位は横1行で表示する。

## 7. グラフのグループ

- 原則：1検査項目＝1グラフ。
- 血圧のみ、収縮期血圧と拡張期血圧を同じグラフに2系列表示。
- 胃部検査：
  - ペプシノゲンⅠ・Ⅱを同じグラフに2系列表示。
  - ペプシノゲン比は別の単独グラフ。

項目色は選択順ではなく`itemCode`に固定。

```text
エメラルド   #08a878
オレンジ     #f07945
ブルー       #3688d8
バイオレット #8a6edb
ピンク       #e55f91
```

## 8. 長期グラフ仕様

- 表示期間は画面内の全グラフ共通。
- 選択肢：5年、10年、全期間。
- 初期表示：10年。
- 「表示期間」と期間ボタンの文字サイズ：16px。
- 長期間はグラフごとに横スクロール。
- 期間変更時、各グラフを最新側へ自動スクロール。
- 横軸はUTCタイムスタンプによる実年月スケール。
- 年月表示形式：`YYYY/MM`（例：`2026/07`）。
- 半年間隔は1年間隔のおよそ半分の距離になる。
- ツールチップ内の年月は日本語表記。
- 欠測時は点を描画しない。
- 欠測区間は無視し、前後の実測値を直線で接続する（`connectNulls`）。
- 12点以下の場合だけ測定値ラベルを常時表示する。

## 9. 縦軸仕様

- 縦軸はグラフ本体と分離し、左端に固定。
- 横スクロールしても数値を残す。
- 固定軸は白背景。フェードは線を隠すため使用しない。
- グラフ本体は左58pxの余白を確保。
- 通常の目盛りは`1・2・5 × 10の累乗`から読みやすい間隔を自動選択。
- 固定縦軸と水平グリッドは同じ目盛値を使用。
- 血圧：グラフ高さを通常の2倍、目盛間隔20mmHg固定。
  - PC：680px
  - iPhone SE2：520px
- 身長：縦軸範囲170〜180cm、2cm間隔固定。

## 10. 基準範囲仕様

- 最新の基準値を全期間へ適用する。
- 基準範囲内ではなく、範囲外の背景だけを薄く着色する。
- 基準内の点：系列色の輪郭＋白抜き。
- 基準値外の点：赤い輪郭＋赤い塗りつぶし。
- サマリーカードには判定ステータスを表示しない。
- 基準値がない項目は通常表示する。

## 11. 分類・実データ項目

分類：

- 身体
- 血圧
- 脂質
- 肝機能
- 腎機能
- 糖代謝
- 尿酸
- 血球
- 尿検査
- 前立腺
- 胃部検査

実データに合わせて追加した主な項目：

- ヘマトクリット
- ChE
- ALP（IFCC）
- ALP（旧測定法）
- 高感度PSA
- ペプシノゲンⅠ
- ペプシノゲンⅡ
- ペプシノゲン比

新旧ALPは測定法と基準値が異なるため別項目。HbA1cは変換時にNGSPを優先し、なければ旧HbA1c列を使用する。

## 12. データ構造と将来方針

内部では共通`itemCode`を使用する。

```text
AST / GOT → ast
ALT / GPT → alt
γ-GTP / γ-GT / GGT → gamma_gtp
```

現在の正規化データ：

```json
{
  "referenceRanges": {
    "systolic": { "lower": null, "upper": 129 }
  },
  "measurements": [
    {
      "examinationDate": "2026-07",
      "values": { "systolic": 120, "diastolic": 74 }
    }
  ]
}
```

将来のAWS構成案：

```text
React / Vite
  → Amplify Hosting
  → Cognito
  → API Gateway
  → Lambda
  → DynamoDB
  → S3 CSVバックアップ
```

- Cognitoは管理者作成の1ユーザー、自己登録禁止。
- 本番公開時は認証なしAPIを公開しない。
- DynamoDBでは1検査項目・1測定値を1レコードとする構想。

## 13. 次の開発候補

画面は一旦完成。次はデータ運用またはAWS側へ進む。

候補：

1. CSV/JSON取込み時のプレビューと詳細な検証
2. 手入力、修正、削除
3. CSV書出し、バックアップ
4. AWSバックエンド
5. Cognito認証
6. Amplify Hosting公開

## 14. GitHub・検証状態

- リポジトリ：`https://github.com/ijenims/health_log.git`
- ブランチ：`main`
- 直近のプッシュ済みコミット：`85cc8ff Improve long-term health chart visualization`
- それ以降の実データ対応・画面改造・`context.md`は未プッシュ。
- 各主要変更後に`npm run build`を実行済み。
- 直近の基準値外プロット変更後もビルド成功。
- Viteのバンドルサイズ警告はあるが、ビルドエラーではない。
